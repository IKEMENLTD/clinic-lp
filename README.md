# Re:Design Clinic LP

オンライン診療（医療ダイエット・ピル・美容内服・エクソソーム・ED・AGA）のランディングページ。医療ダイエット（GIP/GLP-1注射薬）を主訴求とする長尺LP構成。

## 構成

- `index.html` — 単一ページ。CSS/JS込み。ビルド不要の静的サイト
- `assets/img/` — 画像一式（ロゴ・カテゴリ写真・商品写真・診療フロー図）

商品データ（価格・効果・副作用・定期購入プラン）は `index.html` 内の `groups` オブジェクトに集約。価格改定・商品追加はここを編集する。

## 未確定の画像（P-27〜P-30）

長尺化にあたって新設した4枠だけ元素材が存在せず、点線のプレースホルダになっている。枠内の日本語プロンプトを画像生成AIに渡して実画像を作り、`<div class="ph-frame">…</div>` を `<img src="assets/img/…">` に差し替える運用。

| ID | 用途 |
|---|---|
| P-27 | 受付フォトバンド（オンライン予約セクション上部） |
| P-28 | オンライン予約イラスト |
| P-29 | いつもの生活フォトバンド |
| P-30 | 相談クロージング写真 |

これ以外の26点は `assets/img/` の実画像を参照している。

## 画面遷移

ハッシュルーティングで擬似ページ遷移する。

- `#cat-p_xxx` — カテゴリ一覧（例: `#cat-p_himan`）
- `#product-xxx` — 商品詳細（例: `#product-manjaro-25`）
- `#teiki` — 定期購入ページ

## デプロイ

静的ホスティング（Netlify / GitHub Pages 等）にそのまま配置可能。
